<%= form_for(@claass) do |f| %>
  <% if @claass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@claass.errors.count, "error") %> prohibited this claass from being saved:</h2>

      <ul>
      <% @claass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_name %><br />
    <%= f.text_field :class_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
