<%= form_for(@result) do |f| %>
  <% if @result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@result.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
      <% @result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.collection_select :student_id,  Student.order(:student_name), :id, :student_name, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :assessment_id %><br />
    <%= f.collection_select :assessment_id,  Assessment.order(:assessment_name), :id, :assessment_name, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :percentatge %><br />
    <%= f.number_field :percentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
